import 'package:flutter/material.dart';

class StepIntent extends StatelessWidget {
  const StepIntent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
