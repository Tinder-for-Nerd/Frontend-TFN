import 'package:flutter/material.dart';

class ProfileFitScore extends StatelessWidget {
  const ProfileFitScore({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
