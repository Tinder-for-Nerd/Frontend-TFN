import 'package:flutter/material.dart';

class GridDistortion extends StatelessWidget {
  const GridDistortion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
