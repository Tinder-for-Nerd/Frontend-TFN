import 'package:flutter/material.dart';

class LandingSteps extends StatelessWidget {
  const LandingSteps({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
