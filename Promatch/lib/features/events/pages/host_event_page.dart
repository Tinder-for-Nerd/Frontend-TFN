import 'package:flutter/material.dart';

class HostEventPage extends StatelessWidget {
  const HostEventPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Host Event')),
      body: const Center(child: Text('Host Event Page')),
    );
  }
}
