import 'package:flutter/material.dart';

class ProCompanyPage extends StatelessWidget {
  const ProCompanyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Company')),
      body: const Center(child: Text('Pro Company Page')),
    );
  }
}
