import 'package:flutter/material.dart';

class ProAnalyticsPage extends StatelessWidget {
  const ProAnalyticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Analytics')),
      body: const Center(child: Text('Pro Analytics Page')),
    );
  }
}
