import 'package:flutter/material.dart';

class ProjectPostPage extends StatelessWidget {
  const ProjectPostPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Post Project')),
      body: const Center(child: Text('Project Post Page')),
    );
  }
}
