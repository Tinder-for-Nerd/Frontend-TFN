import 'package:flutter/material.dart';

class HiringDashboardPage extends StatelessWidget {
  const HiringDashboardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hiring Dashboard')),
      body: const Center(child: Text('Hiring Dashboard Page')),
    );
  }
}
