import 'package:flutter/material.dart';

class MatchQualityChart extends StatelessWidget {
  const MatchQualityChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
