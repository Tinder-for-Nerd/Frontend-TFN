import 'package:flutter/material.dart';

class SkillDemandChart extends StatelessWidget {
  const SkillDemandChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
