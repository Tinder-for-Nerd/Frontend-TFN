import 'package:flutter/material.dart';

class ProCheckoutPage extends StatelessWidget {
  const ProCheckoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pro Checkout')),
      body: const Center(child: Text('Pro Checkout Page')),
    );
  }
}
