import 'package:flutter/material.dart';

class FreelancerNav extends StatelessWidget {
  const FreelancerNav({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
