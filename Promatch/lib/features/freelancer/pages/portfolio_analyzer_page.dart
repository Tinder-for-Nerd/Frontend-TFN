import 'package:flutter/material.dart';

class PortfolioAnalyzerPage extends StatelessWidget {
  const PortfolioAnalyzerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Portfolio Analyzer')),
      body: const Center(child: Text('Portfolio Analyzer Page')),
    );
  }
}
